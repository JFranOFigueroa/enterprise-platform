# Generated by Ansible - DO NOT EDIT MANUALLY
# Source: applications/{{ app.name }}/app_vars/{{ app.name }}.yml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ app.name }}
  namespace: gitops
spec:
  project: enterprise-platform
  source:
    repoURL: https://github.com/JFranOFigueroa/enterprise-platform.git
    targetRevision: main
    path: {{ app.repoPath }}
    helm:
      valueFiles:
        - {{ app.valuesFile }}
      parameters:
{% for key, value in app_secrets.items() %}
        - name: "{{ key }}"
          value: "{{ value }}"
{% endfor %}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ app.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
