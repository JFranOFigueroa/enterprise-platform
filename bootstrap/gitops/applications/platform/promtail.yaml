apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-promtail
  namespace: gitops
spec:
  project: default
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: promtail
      targetRevision: 6.16.6
    - repoURL: https://github.com/JFranOFigueroa/enterprise-platform.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: platform-logging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  helm:
    valueFiles:
      - $values/platform/logging/promtail-values.yaml
