#!/usr/bin/env bash
# Enterprise Platform - Install prerequisites on the host machine
set -euo pipefail
echo "Prerequisites check - ensure VMware Workstation Player and Vagrant are installed."
echo "Also ensure the vagrant-vmware-desktop plugin is installed:"
echo "  vagrant plugin install vagrant-vmware-desktop"
